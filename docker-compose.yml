services:
  front:
    container_name: neogaming-front
    build:
      context: .
      dockerfile: Dockerfile
    image: neogaming-front:latest
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
    restart: unless-stopped
